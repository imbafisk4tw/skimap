# Secrets / local config
.env
config.js

# Node
node_modules/

# Local caches / runtime
.cache/
db/
*.log

# Not needed in repo
old/
tools/

# Optional: große OSM Dateien (falls du die lokal hast)
*.osm.pbf
*.pbf

# OSRM build artifacts (sehr groß) – nur lokal nötig
osrm*/
*.osrm*
*.osrm.*
